<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SoapBox.Demo.PinBall"
    x:Class="SoapBox.Demo.PinBall.PinBallLauncherView">

    <!-- Render a geometry as a filled in path, by default -->
    <DataTemplate DataType="{x:Type local:PinBallLauncherPlungerSprite}">
        <Path Stroke="Black" StrokeThickness="1" Data="{Binding Path=Geometry}">
            <Path.Fill>
                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop Offset="0" Color="Black"/>
                    <GradientStop Offset="0.4" Color="Silver"/>
                    <GradientStop Offset="1" Color="Black"/>
                </LinearGradientBrush>
            </Path.Fill>
        </Path>
    </DataTemplate>

    <!-- Make it silver -->
    <DataTemplate DataType="{x:Type local:PinBallLauncherGuideSprite}">
        <Path Data="{Binding Path=Geometry}" Fill="Silver">

        </Path>
    </DataTemplate>

</ResourceDictionary>